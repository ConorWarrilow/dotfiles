# -------------------------------------------------------------------
# Work profile overrides and tooling
# Source this file by adding 'work' to the sources array in .zshrc
# (done automatically when profile=work in .chezmoi.toml)
# -------------------------------------------------------------------

# Work-specific git identity reminder
# alias git-work='git config user.email "your.work@company.com"'

# VPN helpers (uncomment/adapt as needed)
# alias vpn-up='sudo openconnect vpn.company.com'
# alias vpn-down='sudo pkill openconnect'

# Internal tooling PATH additions
# export PATH="$PATH:/opt/company/bin"

# Kubernetes context aliases
# alias kctx='kubectl config use-context'
# alias kns='kubectl config set-context --current --namespace'

# AWS profile override for work account
# export AWS_PROFILE="work"

# Additional sourcing for work machine
# [[ -f "$HOME/.zshrc.work" ]] && source "$HOME/.zshrc.work"
